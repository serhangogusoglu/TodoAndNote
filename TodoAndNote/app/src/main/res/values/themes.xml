<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TodoAndNote" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/powderBlue</item>
         <item name="colorPrimaryVariant">@color/powderBlue</item>
         <item name="android:statusBarColor">@color/powderBlue</item>
    </style>

    <style name="Theme.TodoAndNote" parent="Base.Theme.TodoAndNote" />
</resources>